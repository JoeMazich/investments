import './App.scss'

import { Button } from '@components'

const App: React.FC = () => {
  return (
    <div>
      <h1>Vite + React</h1>
      <Button variant="contained">Click Me</Button>
    </div>
  )
}

export default App
