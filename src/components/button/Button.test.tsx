import '@testing-library/jest-dom'

describe('Button Component', () => {
  test("renders the text 'Vite + React'", () => {
    expect(true).toBe(true) // Placeholder for Button component test
  })
})
